#ifndef ARRAY_H
#define ARRAY_H

// Custom data type that stores an array size and its data pointer.
struct _my_array
{
    int size;
    double *data;
};

typedef struct _my_array Array;

#endif







